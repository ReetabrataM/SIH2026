let last='';const guard=document.createElement('button');guard.className='safescroll-guard';guard.textContent='● AI Guard';guard.setAttribute('aria-label','SafeScroll AI Guard');document.documentElement.append(guard);const panel=document.createElement('aside');panel.className='safescroll-panel';panel.hidden=true;document.documentElement.append(panel);guard.onclick=()=>panel.hidden=!panel.hidden;
function render(x){const w=x.warning;guard.className=`safescroll-guard ${w?.severity||'safe'}`;guard.textContent=`● AI Guard${w?' · Attention':''}`;panel.innerHTML=`<b>${w?'Potential concern':'AI Guard'}</b><p>${w?.message||x.analysis?.explanation||'Monitoring browser-visible page metadata only.'}</p>${x.analysis?`<small>Confidence: ${Math.round(x.analysis.confidence*100)}% · ${x.analysis.claim}</small>`:''}`;if(w)panel.hidden=false;}
function scan(){const title=document.title,description=document.querySelector('meta[name="description"]')?.content||'';const key=`${location.href}|${title}`;if(!title||key===last)return;last=key;chrome.runtime.sendMessage({type:'content-event',title,url:location.href,description,contentIdentifier:location.pathname},render);}
new MutationObserver(()=>setTimeout(scan,500)).observe(document.documentElement,{subtree:true,childList:true});scan();chrome.runtime.onMessage.addListener(m=>{if(m.type==='guard-state'){guard.className=`safescroll-guard ${m.state}`;guard.textContent=m.state==='offline'?'● AI Guard · Sign in':'● AI Guard';}});
